# 🏗️ System Architecture

Complete architecture overview of the retail inventory management system with Waziper WhatsApp integration.

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│   │  POS Desktop │  │   Web App    │  │   Mobile     │             │
│   │   (Offline)  │  │   (Online)   │  │   (Future)   │             │
│   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘             │
│          │                 │                  │                      │
│          └─────────────────┼──────────────────┘                      │
│                            │                                         │
│                    ┌───────▼────────┐                                │
│                    │  Event Queue   │                                │
│                    │   (IndexedDB)  │                                │
│                    └───────┬────────┘                                │
└────────────────────────────┼──────────────────────────────────────...
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                   SUPABASE BACKEND                                  │
├────────────────────────────┼──────────────────────────────────────┤
│                            │                                        │
│                    ┌───────▼────────┐                               │
│                    │  Edge Function │                               │
│                    │   sync_event   │                               │
│                    └───────┬────────┘                               │
│                            │                                        │
│              ┌─────────────┼─────────────┐                          │
│              │             │             │                          │
│       ┌──────▼──────┐ ┌───▼────┐ ┌─────▼──────┐                   │
│       │ Validation  │ │  RLS   │ │ Stock Check│                   │
│       │   Logic     │ │ Checks │ │  (Prevent  │                   │
│       │             │ │        │ │  Negative) │                   │
│       └──────┬──────┘ └───┬────┘ └─────┬──────┘                   │
│              │             │             │                          │
│              └─────────────┼─────────────┘                          │
│                            │                                        │
│                    ┌───────▼────────┐                               │
│                    │   PostgreSQL   │                               │
│                    │    Database    │                               │
│                    └───────┬────────┘                               │
│                            │                                        │
│         ┌──────────────────┼──────────────────┐                    │
│         │                  │                  │                    │
│  ┌──────▼──────┐  ┌────────▼─────┐  ┌────────▼────────┐           │
│  │ event_ledger│  │current_stock │  │ Report Views   │           │
│  │ (Immutable) │  │    _view     │  │ (Aggregated)   │           │
│  └─────────────┘  └──────────────┘  └────────────────┘           │
│                                                                     │
└─────────────────────────────────────────────────────────────────...

┌─────────────────────────────────────────────────────────────────────┐
│                    EXTERNAL INTEGRATIONS                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────┐              ┌──────────────────┐             │
│  │  Waziper Panel   │              │   Amazon API     │             │
│  │  (WhatsApp API)  │              │  (Marketplace)   │             │
│  └────────┬─────────┘              └────────┬─────────┘             │
│           │                                 │                        │
│           │ Webhook                         │ API Calls              │
│           │                                 │                        │
│  ┌────────▼──────────┐            ┌────────▼─────────┐              │
│  │  Edge Function    │            │  Edge Function   │              │
│  │ waziper-webhook   │            │  amazon-sync     │              │
│  └────────┬──────────┘            └────────┬─────────┘              │
│           │                                 │                        │
│           └─────────────┬───────────────────┘                        │
│                         │                                            │
│                ┌────────▼────────┐                                   │
│                │   Database      │                                   │
│                │  (Supabase)     │                                   │
│                └─────────────────┘                                   │
│                                                                       │
└───────────────────────────────────────────────────────────────────...
```

## 🗄️ Database Schema (Simplified)

```
┌──────────────────┐
│    products      │
├──────────────────┤
│ id (PK)          │
│ name             │
│ category         │
│ company          │
└────────┬─────────┘
         │ 1
         │
         │ *
┌────────▼─────────┐
│    variants      │
├──────────────────┤
│ id (PK)          │
│ product_id (FK)  │
│ size             │
│ color            │
│ sku_code         │
│ mrp              │
│ selling_price    │
└────────┬─────────┘
         │ *
         │
         │ 1
┌────────▼─────────────────────────────────────────┐
│            event_ledger                          │
│         (INSERT-ONLY / IMMUTABLE)                │
├──────────────────────────────────────────────────┤
│ event_id (PK)                                    │
│ event_type (PURCHASE|SALE|TRANSFER|etc)         │
│ variant_id (FK)                                  │
│ quantity (+/-)                                   │
│ from_location_id (FK)                            │
│ to_location_id (FK)                              │
│ reference_number                                 │
│ created_at                                       │
│ created_by (FK)                                  │
│                                                  │
│ ⚠️  NO UPDATE/DELETE ALLOWED (Trigger Protected)│
└──────────────────┬───────────────────────────────┘
                   │
                   │ Derives
                   │
         ┌─────────▼──────────┐
         │ current_stock_view │
         ├────────────────────┤
         │ variant_id         │
         │ location_id        │
         │ current_quantity   │  ← SUM(events)
         │                    │
         │ ✅ ALWAYS ACCURATE │
         └────────────────────┘
```

## 🔄 Event Flow (POS → Database)

```
1. OFFLINE MODE
   ┌──────────────┐
   │ POS Terminal │
   │  (Browser)   │
   └──────┬───────┘
          │ Create Sale
          │
          ▼
   ┌──────────────┐
   │ IndexedDB    │  ← Events queued locally
   │ Event Queue  │     (Internet down)
   └──────────────┘

2. ONLINE SYNC
   ┌──────────────┐
   │ IndexedDB    │
   │ Event Queue  │
   └──────┬───────┘
          │ Batch sync
          │ when online
          ▼
   ┌──────────────────┐
   │  Edge Function   │
   │   sync_event     │
   └──────┬───────────┘
          │
          ├─→ Validate event
          ├─→ Check permissions (RLS)
          ├─→ Verify stock (prevent negative)
          ├─→ Check idempotency (duplicate event_id)
          │
          ▼
   ┌──────────────────┐
   │  event_ledger    │  ← INSERT (append-only)
   └──────┬───────────┘
          │
          │ Triggers
          │
          ▼
   ┌──────────────────┐
   │ current_stock    │  ← Auto-recalculated
   │     _view        │
   └──────────────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────┐
│              REQUEST FLOW                       │
└─────────────────────────────────────────────────┘

1. CLIENT REQUEST
   ↓
2. JWT VALIDATION (Supabase Auth)
   ├─ Valid? → Continue
   └─ Invalid? → 401 Unauthorized
   ↓
3. EDGE FUNCTION (sync_event)
   ├─ Validate event structure
   ├─ Check required fields
   └─ Verify business logic
   ↓
4. RLS POLICIES (Database Level)
   ├─ User role check
   ├─ Location access check
   └─ Data isolation
   ↓
5. CONSTRAINTS & TRIGGERS
   ├─ Foreign key validation
   ├─ NOT NULL enforcement
   ├─ Negative stock prevention
   └─ Immutability (UPDATE/DELETE blocked)
   ↓
6. AUDIT LOG
   └─ Record all actions
```

## 📱 WhatsApp Integration Flow

```
CUSTOMER                 WAZIPER              SUPABASE              DATABASE
   │                       │                      │                     │
   │  "ORDER INV-123"      │                      │                     │
   ├──────────────────────►│                      │                     │
   │                       │  Webhook POST        │                     │
   │                       ├─────────────────────►│                     │
   │                       │                      │  Check opt-in       │
   │                       │                      ├────────────────────►│
   │                       │                      │◄────────────────────┤
   │                       │                      │  opted_in = true    │
   │                       │                      │                     │
   │                       │                      │  Query order        │
   │                       │                      ├────────────────────►│
   │                       │                      │◄────────────────────┤
   │                       │                      │  Order details      │
   │                       │                      │                     │
   │                       │  Send response       │  Log interaction    │
   │                       │◄─────────────────────┤────────────────────►│
   │                       │                      │                     │
   │  "✅ Order Status..."│                      │                     │
   │◄──────────────────────┤                      │                     │
   │                       │                      │                     │
```

## 📊 Report Generation

```
┌────────────────────────────────────────────────────┐
│              REPORTS ARCHITECTURE                   │
└────────────────────────────────────────────────────┘

event_ledger (Raw Events)
         │
         │ Aggregate
         │
         ├─────────────┬─────────────┬──────────────┬─────────────┐
         │             │             │              │             │
         ▼             ▼             ▼              ▼             ▼
  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │  daily   │  │ monthly  │  │   size   │  │outstanding│ │   dead   │
  │  sales   │  │ product  │  │  demand  │  │  ledger  │  │  stock   │
  │  report  │  │performance│  │  report  │  │  report  │  │  report  │
  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘

  ✅ Real-time   ✅ Accurate    ✅ No manual   ✅ Auto      ✅ Insights
     updates        always         updates     calculated
```

## 🔄 Offline Sync Strategy

```
┌────────────────────────────────────────────────────┐
│           OFFLINE-FIRST ARCHITECTURE                │
└────────────────────────────────────────────────────┘

INTERNET AVAILABLE
   POS → Edge Function → Database ✅ Instant

INTERNET DOWN
   POS → IndexedDB Queue ✅ Never blocks billing
           │
           │ (Store locally)
           │
           ▼
   ┌────────────────┐
   │ Queued Events  │
   │ event_id: uuid │  ← Client-generated UUID
   │ timestamp: now │     ensures uniqueness
   │ retries: 0     │
   └────────────────┘

INTERNET RESTORED
   IndexedDB Queue
           │
           │ Batch sync
           │ with retry logic
           ▼
   Edge Function
           │
           ├─→ Idempotency check (event_id)
           ├─→ Stock validation
           └─→ Insert if valid
           
   ✅ No duplicates (UUID prevents)
   ✅ Order preserved
   ✅ Data integrity maintained
```

## 🎯 Scalability

### Horizontal Scaling
```
Load Balancer
      │
      ├─→ POS Terminal 1
      ├─→ POS Terminal 2
      ├─→ POS Terminal 3
      └─→ POS Terminal N
            │
            └─→ Edge Functions (Auto-scale)
                      │
                      └─→ PostgreSQL (Connection pooling)
```

### Performance Optimizations
1. **Indexes**
   - `event_ledger.variant_id`
   - `event_ledger.created_at`
   - `event_ledger.reference_number`

2. **Materialized Views** (Future)
   - Pre-calculate daily aggregates
   - Refresh hourly

3. **Caching** (Future)
   - Redis for hot data
   - CDN for static assets

## 🛡️ Disaster Recovery

```
┌────────────────────────────────────────────────────┐
│             BACKUP & RECOVERY STRATEGY              │
└────────────────────────────────────────────────────┘

PREVENTION
   ├─ Immutable event_ledger ✅
   ├─ No DELETE/UPDATE allowed ✅
   ├─ Audit logs for all actions ✅
   └─ RLS for data isolation ✅

BACKUPS
   ├─ Supabase auto-backups (Daily)
   ├─ Point-in-time recovery (7 days)
   └─ Export critical data (Weekly)

RECOVERY
   ├─ Restore from backup
   ├─ Replay events if needed
   └─ Recalculate views from ledger
```

## 📈 Monitoring & Observability

```
┌────────────────────────────────────────────────────┐
│              MONITORING POINTS                      │
└────────────────────────────────────────────────────┘

1. EDGE FUNCTIONS
   ├─ Invocation count
   ├─ Error rate
   ├─ Latency (p95, p99)
   └─ Logs (Supabase Dashboard)

2. DATABASE
   ├─ Query performance
   ├─ Connection pool usage
   ├─ Slow queries
   └─ Table sizes

3. BUSINESS METRICS
   ├─ Daily sales
   ├─ Stock movements
   ├─ Sync failures
   └─ User activity

4. WAZIPER
   ├─ Message delivery rate
   ├─ Response time
   ├─ Failed messages
   └─ Opt-in/out rates
```

## 🚀 Future Enhancements

1. **Real-time Stock Updates** (WebSocket)
2. **Advanced Analytics** (Machine Learning)
3. **Mobile App** (React Native)
4. **Multi-currency Support**
5. **Loyalty Program Integration**
6. **AI-powered Demand Forecasting**

---

## 🎯 Key Design Principles

✅ **Event-Driven** - All changes are events  
✅ **Immutable Ledger** - History is preserved  
✅ **Offline-First** - Never blocks operations  
✅ **Derived State** - Stock calculated, not stored  
✅ **Role-Based Security** - RLS enforced  
✅ **Audit Trail** - Complete tracking  

**This architecture ensures data integrity, scalability, and reliability! 🚀**
